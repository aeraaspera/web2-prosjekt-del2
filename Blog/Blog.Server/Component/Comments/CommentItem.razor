@using Blog.Common.Model.Dto
@using Blog.Common.Model.ViewModel

<MudCard Style="@($"background-color:white;")">
    <MudCardContent>
        <MudText Typo="Typo.body2" Align="Align.Start" Style="@($"color:#6082B6;")">@CommentModel.UserName</MudText>
        <MudText Typo="Typo.body2" Align="Align.Start">@CommentModel.DateCreated</MudText>
        <MudText Typo="Typo.body2" Align="Align.Start">@CommentModel.CommentTitle</MudText>
        <MudText Typo="Typo.body2" Align="Align.Start">@CommentModel.CommentDetails</MudText>
    </MudCardContent>
    <hr />
</MudCard>

@code {
    [Parameter]
    public CommentModel CommentModel { get; set; }

    protected override void OnInitialized()
    {
        Console.WriteLine($"CommentItem Initialized - CommentID: {CommentModel.CommentID}");
        base.OnInitialized();
    }
}